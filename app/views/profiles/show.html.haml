.container
  .row.mb-4
    = link_to "Edit profile", edit_profile_path
  - if @profile_widgets.any?
    .row
      - @profile_widgets.each do |widget|
        .col-sm-6.col-md-4
          = render widget
  - else
    %h2.text-center.text-muted No widgets to display