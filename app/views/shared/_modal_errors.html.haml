%div{ :class => "alert alert-danger modal-errors" }
  %h5.alert-heading There were errors that prevented this #{object.class.to_s.underscore.humanize.downcase} from being saved:
  %ul
    - object.errors.full_messages.map do |msg|
      %li= msg